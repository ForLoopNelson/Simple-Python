# initialize a list of integers
num_list = [1, 5, 4, 45, 12]
# initialize variable to hold the sum of list elements
total = 0
# iterate over the list
for num in num_list:
    # add current list element to the sum
    total = total + num
print(f"Sum of elements of list is {total}")
